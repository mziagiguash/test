// next.config.ts
import type { NextConfig } from "next"; // Импорт типа

const nextConfig: NextConfig = {
  // Ваши настройки конфигурации
  reactStrictMode: true, // Пример конфигурации
};

export default nextConfig;
